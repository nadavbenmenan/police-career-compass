/* ---------------------------------------------------------------------------
   data/config.js — דגלי הגדרה.
   נטען ראשון, לפני roles/questions/scoring/app.
   --------------------------------------------------------------------------- */

const CONFIG = {
  APP_NAME: "מצפן הקריירה",
  APP_SUBTITLE: "כמה שאלות קצרות — ונמצא לך את התפקידים שהכי מתאימים לך במשטרת ישראל.",

  // באנר הכנות. מוצג בפתיחה ובתוצאות (§7.3).
  DISCLAIMER: "כלי הערכה עצמית אינדיקטיבי בלבד. הקביעה הרשמית נעשית רק בתהליך המיון במרכז הגיוס.",

  // הטקסט המדויק שמוצג כאשר salary === null. אין להמציא טווח (§7.2).
  SALARY_UNKNOWN: "נמסר ביום המיון",

  // כמה תפקידים מוצגים במסך התוצאות.
  TOP_N: 3,

  // סף מינימלי לדירוג (§5.5) — מונע מתפקיד נישה עם מעט שאלות תורמות לקפוץ ל-100%.
  MIN_EARNED_FOR_RANK: 4,

  // תפקידים שנחסמו בשער יוצגו בבלוק השקיפות רק אם היו מגיעים לאחוז הזה ומעלה.
  BLOCKED_MIN_PCT: 50,

  // השלמה ל-3 המלצות כשאין מספיק מועמדים ("תמיד ממליץ", §5.5). לכולם gates ריק.
  FALLBACK_ROLE_IDS: ["patrol", "dispatcher", "detective", "investigator", "traffic_patrol"],

  // השלמה מקבילה למי שנותב למסלול המנהלה.
  ADMIN_FALLBACK_ROLE_IDS: ["admin_office", "admin_logistics", "admin_welfare", "admin_training"],

  // כמה משלוש המשבצות שמורות לתפקידי ליבה (תיקון 1: "בעיקר תפקידי ליבה").
  // 2 מתוך 3 — המשבצת השלישית נשארת פתוחה להתמחות שבאמת התאימה.
  CORE_MIN_SLOTS: 2,

  // פער האחוזים שמשל"ט/סייר תנועה צריכים כדי לקחת את משבצת הליבה השנייה
  // מחוקר/בלש/סייר. כויל בסימולציה (test/simulate.js) כדי שהליבה הראשית
  // תישאר 80–90% מהמשבצות, ובמקביל משל"ט יופיע הרבה.
  CORE_SECONDARY_MARGIN: 16,

  // כמה תפקידים ייחודיים להציג בבלוק "גם התאימו לך" מתחת לשלישייה.
  ALSO_FIT_MAX: 4,

  // מספר הנימוקים המקסימלי לכל תפקיד (§5.6).
  MAX_REASONS: 4,

  // השהיה במילישניות לפני מעבר אוטומטי לשאלה הבאה אחרי בחירה.
  AUTO_ADVANCE_MS: 260,

  // שכבת הסבר AI — כבויה כברירת מחדל. הדגמת GitHub Pages רצה תמיד דטרמיניסטית (§8).
  ENABLE_AI_EXPLANATION: false,
  AI_PROXY_URL: "http://127.0.0.1:5055/api/explain",

  // מריץ את בדיקות השפיות של §5.7 לקונסול. כבוי בייצור.
  DEBUG: false
};

if (typeof module !== "undefined" && module.exports) module.exports = { CONFIG };
